// Export all mutation hooks
export {
  useLoginMutation,
  useRegisterAuthorMutation,
  useRegisterInstitutionMutation,
  useLogoutMutation,
  useRefreshTokenMutation,
} from "./mutations";

// Export all query hooks when created
// export * from './queries';
